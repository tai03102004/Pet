mixin search()

    .header__search
        form(
            action=`/search`
            method="GET"
        )
            button(type="submit", class="btn btn-search" )
                i(class="fa fa-search")
            input(type="text" placeholder="Nhập từ khoá..." name="keyword", class="form-control",value=keyword) 